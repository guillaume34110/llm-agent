# Required GitHub Secrets

Set these in Settings → Secrets and variables → Actions:

| Secret | Description | Example |
|--------|-------------|---------|
| `BACKEND_URL` | Deployed backend URL (no trailing slash) | `https://api.progsoft.ai` |
| `ADMIN_KEY` | Value of `ADMIN_KEY` env var on backend | `prod-admin-key-...` |
